# Wayward feedback list

## INSTRUCTIONS
This is the running log of change requests and new features for the project. How to work it:

- **Pick up tasks from the top down**, but use judgement — group related items when it's more efficient.
- When you finish a task, **flip its checkbox** (`[ ]` → `[x]`) and write a short **Done** note directly under it: what actually changed, which files, and any follow-ups or deliberate omissions. Include the **commit ID**.
- **Commit and push** each finished task (or a coherent batch) to the working branch.
- If a task turns out to be infeasible, ambiguous, or a bad idea, **say so under the task** instead of silently skipping it — note why and what you'd do instead.
- **Keep the format**: one task per entry, `[ ]`/`[x]` checkbox, separated by `---`. **Newest tasks go at the bottom.**
- Related tasks are grouped under `###` sub-headings for readability; keep new items near their theme when it's obvious, otherwise append at the end.

> The full history of completed work lives in `TODO_old.md`.

## FEEDBACK ITEMS

### Config & the Editor
---
[x] Restrict the Editor so it can only edit the **Custom Instructions** (the free-text field within Story Style / Narrator Instructions), never the core Narrator Instructions.
  Done: removed the Editor's `set_narrator_instructions`/`get_narrator_instructions` tools + handlers from `server/ai/planner.py`. The Editor now shapes narration guidance through `set_story_style`'s `customInstructions` field (already supported) instead of overwriting the core Narrator role/behaviour. Updated the PLANNER_GUIDANCE (CONSISTENCY / SENSITIVE OVERWRITES lines) and module docstring to reflect the restriction, and the `prompt_builder.py` comment noting `NarratorConfig.instructions` is now a legacy override the Editor no longer writes. Commit: 3358014.

---
[x] Make the Editor's changes apply **live** in the UI. Right now most edits only show up after a refresh or an app relaunch.
  Done: `refreshWorldPanels()` (`client/src/state/chatStore.ts`, run after every planner turn + delete-apply) already refetched lore/tasks/party/items/inventory/narrator, but **not** the structured-config stores the Editor edits via `set_scenario`/`set_story_style`/`set_world_rules` — those went stale until reload. Added `useScenarioStore.fetchScenario()`, `useStoryStyleStore.fetchFields()`, and `useCampaignRulesStore.fetchRules()` to the refresh. Commit: 3358014.

---
[x] Allow a **separate model** to be assigned to the Editor (the same way the Chronicler and summariser already support model overrides).
  Done: added `OpenRouterSettings.planner_model_id` (app-scoped, blank → main model) with an additive ALTER migration (`database.py`), wired through `schemas.py`/`settings.py` as `plannerModelId`, resolved in `run_planner_agent` (`planner.py`) exactly like the Chronicler's `worldbuilding_model_id`. Client: `plannerModelId` on the shared type + `settingsStore`, and a new **Editor** subsection (Global, with a `ModelPicker`) in Config → AI & Model. Commit: 3358014.

### Models, parameters & reasoning
---
[x] Clarify whether the **custom sampling parameters actually take effect when Reasoning is enabled**, and add a way to **turn Reasoning off**.
  Done: (1) **Clarification** — sampling params DO take effect with reasoning on. In `_apply_sampling` (`server/ai/openrouter.py`) the sampling keys (`top_p`/`min_p`/`top_k`/penalties) and the `reasoning` key are independent entries on the same request body; nothing suppresses sampling when reasoning is set. (Any clamping of `temperature`/`top_p` during a thinking phase is provider-side, not Wayward's.) (2) **Turn Reasoning off** — the backend already supported an `"off"` sentinel (`reasoning:{enabled:false}`) used only by the narrator's budget recovery; now surfaced as an explicit **Off** option in Config → AI & Model → Reasoning Effort (distinct from "Provider default", which leaves the model's own setting untouched). Accepted server-side (`settings.py` whitelist now includes `off`) and round-trip tested. Commit: e3d8bb5. Follow-up (separate task, not done here): mobile slider ergonomics + a sampling-only "Reset to default parameters" — see the next item.

---
[x] Make **adjusting sampling parameters usable on mobile** (currently difficult), and add a **"Reset to default parameters"** option.
  Done: the shared `Slider` (Config → AI & Model → Sampling, `client/src/components/Settings/SettingsPanel.tsx`) now pairs the thin `type="range"` track with a right-aligned **number input** (clamped to each param's min/max, `inputMode="decimal"`) — the field is the reliable way to set an exact value on touch, where dragging a hairline slider is fiddly. Added a **"RESET TO DEFAULT PARAMETERS"** button in the Sampling subsection (`resetSampling`) that restores only the sampling knobs (temperature/top-p/min-p/top-k/penalties) to defaults, leaving Max Tokens and Reasoning untouched. Commit: d62d41c.

---
[ ] Build a **more reliable tool-calling path**. The best *narrative* models aren't necessarily the newest, so they often struggle with complex formatting and tool calling — we need a system that keeps them working well regardless.
  Progress (front-loadable slice done; broader convergence still open): shipped the two highest-value pieces. (1) **Tool Mode** (`OpenRouterSettings.tool_mode`, Config → Agents & Tools): **Auto** (native tools if the model supports them, else the text protocol), **Native** (force the tool loop), **Text protocol** (force `<<<ACTIONS>>>` — reliable on strong narrative models weak at tool calling), **Off** (pure prose). Replaces the old binary that always sent a tool-capable-flagged model down the native path even when it called tools badly; `_resolve_narration_mode` (`server/api/chat.py`) resolves it, additive migration seeds it from the legacy `use_tools`. (2) **Hardened the `<<<ACTIONS>>>` text parser** (`parse_action_block`, `narrator_actions.py`): tolerant markers (spacing/casing), accepts a missing END marker, salvages the JSON via brace-matching, and ALWAYS strips the block from prose so malformed JSON can't leak. Also added **validate-and-repair** for native calls — malformed argument JSON is reported back to the model to resend instead of running with silent empty args (`narrator_agent.py`). Commits: 9735b0c, e3d8bb5, f4e83f0. **Still open** (deliberately not done): unifying the two paths onto one forgiving protocol with native as an accelerator, and shrinking the per-turn tool surface (carry scene state in a trailing block instead of a tool round). Those are the larger redesign; revisit as a dedicated task.

---
[x] Fix models (notably the **DeepSeek** family, which is preferred for narration) that write out a reply, delete it, and immediately write a follow-up that continues the deleted text.
  Done: two distinct root causes, both fixed. (1) **Preamble discard in the agentic loop** (the primary cause) — DeepSeek narrates the whole beat and THEN appends a state-write tool call in the same message; the loop treated that prose as throwaway "preamble", discarded it from the player's view, KEPT it in the transcript, and forced a re-narration — so the next round *continued* text the player could no longer see. Now: when the streamed content is a substantial narration and every requested tool is a non-prose-shaping safe write (`set_scene`/`grant`/`remove`/`consume`/`equip`/`unequip`), the loop keeps the beat and stops — no re-narration (`_SAFE_WRITE_TOOLS`, `narrator_agent.py`); and when content genuinely IS preamble and is discarded, it's dropped from the transcript too (`content=None`) so the model re-narrates cleanly instead of continuing deleted text. `skill_check`/read tools still force a clean redo (their result must be free to shape the prose). (2) **Inline `<think>…</think>` leak** — some DeepSeek deployments (custom/NIM providers, or providers without a reasoning parser) emit chain-of-thought inline in `delta.content` instead of a dedicated reasoning field, so it rendered as narration and then the real answer followed. Added a streaming-aware `_ThinkStripper` (`openrouter.py`) that routes a LEADING think block to the reasoning channel in both stream parsers, tolerant of a tag split across deltas; only a leading block is stripped, so ordinary prose containing the token is untouched. Commits: f92da11, f4e83f0. Tests: `test_think_stripping.py`, `test_narrator_preamble.py`.

### The Chronicler & the Lorebook
---
[ ] Make the Chronicler's lore updates **additive** — append new information (with timestamps) instead of replacing an entry's whole content — and only touch an entry **when there is genuinely something new to add**, rather than rewriting the same entries every turn.

---
[ ] Give **item creation clearer, deterministic logic**. For example: only **bolded** words become Lore entries (whether an item or something else), and the Narrator gets explicit rules for **what to bold**. The Chronicler currently struggles to create items reliably.

### Tasks, Objectives & narrative direction
---
[x] Add **Objectives** alongside Tasks — larger, direction-setting goals that steer the Narrator (e.g. "Gather a party of five", "Defeat the Demon Queen before the next Blood Moon rises"). Bigger than a Task. Look to **Dungeon World's Fronts, Stakes, and Portents** for inspiration.
  Done: new adventure-scoped **`Objective`** model (`text`, `status` active/completed/failed, `detail` — the free-text "stakes" slot for Fronts-style impending threats), with an idempotent CREATE-TABLE migration + `objectives.status` index (`server/db/database.py`). REST CRUD at `/objectives` (`server/api/objectives.py`, registered in `routes.py`); schemas in `schemas.py`. **Narrator injection**: `build_prompt` now emits an `OVERARCHING OBJECTIVES` block (active only, with detail) *before* the task list, telling the narrator to steer toward them and not resolve them cheaply — threaded through `_load_game_context`/`_maybe_summarize_and_build` in `chat.py`. **Editor** gets `create_objective`/`update_objective`/`delete_objective` tools (`planner.py`; delete is queued for confirmation like other deletes — `planner.py` API handles the new `objective` kind); objectives also listed in the Editor's world-state context. **Client**: `objectivesStore`, shared `Objective` type, and an inline-editable **OBJECTIVES** section at the top of the Tasks panel (`ObjectivesSection.tsx`); wired into App boot, `reloadAll`, and the Editor's live `refreshWorldPanels`. Deliberately NOT wired into the Chronicler (objectives are big, deliberate goals better set by the player/Editor than auto-proposed). Commit: d62d41c.

---
[x] Add a player **Wishlist** the Narrator keeps in mind (e.g. "I want to recruit an Elf to my party"), optionally with a **priority rating** per wish.
  Done: new adventure-scoped **`Wish`** model (`text`, `priority` 0 normal / 1 low / 2 medium / 3 high) with CREATE-TABLE migration (`database.py`). REST CRUD at `/wishes` (`server/api/wishlist.py`, registered in `routes.py`; list sorted high-priority-first; priority clamped 0-3). **Narrator injection**: `build_prompt` emits a `PLAYER WISHLIST` block (with the priority label when set) framed as a *soft steer* — "weave in naturally when the story allows; never force them". Player-authored ONLY — the Chronicler/Editor never touch it (unlike Tasks/Objectives), so it stays the player's private want-list. **Client**: `wishlistStore`, shared `Wish`/`WishPriority` types, and an inline **WISHLIST** section at the bottom of the Tasks panel (`WishlistSection.tsx`) with a click-to-cycle priority chip (—/LOW/MED/HIGH); wired into App boot + `reloadAll`. Commit: d62d41c.

---
[x] Make a **Task's Notes visible to the Narrator** and **editable by the Chronicler and the Editor**.
  Done: (1) **Visible to the Narrator** — `build_prompt`'s `ACTIVE TASKS` block now appends each task's `notes` (as an indented `Notes:` line) so context the player/Chronicler/Editor recorded reaches the narration. (2) **Editor** — `create_task` and `update_task` gained a `notes` param (schema + handler, `planner.py`); the Editor guidance explains notes are context the narrator reads. (3) **Chronicler** — `create_task` gained optional `notes`; the old `update_task_status` tool became **`update_task`** which now takes an optional `notes` string that is **appended** additively (new line, prior notes kept) alongside the status change — status is now optional so a note-only update is allowed. Proposal apply/reverse snapshot both `status` and `notes` so swipe/regenerate/delete restore correctly (`worldbuilder.py`). The Task Inspector already edited notes client-side, so no UI change was needed. Commit: d62d41c.

### Action Suggestions
---
[x] Simplify Action Suggestions to a **single shared instruction** for all options. Drop the per-option instructions — situations vary too much for universal per-slot rules to work well.
  Done: dropped the per-slot **Option Rules** as the shaping mechanism. The single shared **Suggestion Instructions** field (`action_suggestions_instructions`, already existed) now shapes every option, and a new **`action_suggestions_count`** (1-6, default 4) controls how many to generate. `action_suggester.py`: `ACTION_SUGGESTIONS_GUIDANCE` and `build_inline_options_guidance` no longer emit an `OPTION RULES` block — they ask for N options and explicitly tell the model to keep the *set* varied (spread across cautious/bold, kind/ruthless, etc.); added `normalize_suggestions_count`. Inline mode (`chat.py`) switched from `_inline_option_rules` → `_inline_option_count`. Wire: `actionSuggestionsCount` on the narrator schema/store/Config UI (a slider+number replacing the add/remove rule-slot editor). The legacy `action_option_rules` column is kept only for back-compat; an additive migration seeds `action_suggestions_count` from `len(rules)` so a campaign that authored e.g. 5 rules keeps getting 5 options. Commit: d62d41c.

### Performance & state ownership (from the Wayward / Loom / Aimon comparison)
---
[x] Cut the per-turn cost and take the state the narrator shouldn't own. See `docs/wayward-vs-loom-vs-aimon.md` for the analysis this came from.
  Done, as one coherent change:

  **Per-turn model calls: 2–8 → 1–2.**
  - `NarratorConfig.action_suggestions_mode` now defaults to **`inline`** — the options ride the narrator's trailing block instead of buying a second generation every turn. `separate` stays as the reroll/self-heal path (which is what it's good at).
  - **`set_scene` left the native tool schema.** It cost a whole round-trip (call → execute → tool result → call again to narrate) to write four strings the model could append to the beat it had just written. Scene now rides the new trailing **`<<<TURN>>>`** block (`server/ai/turn_block.py`) with the options — one marker to teach, one thing to parse, one thing to strip. The handler survives for the legacy text protocol only.
  - `max_tool_rounds` default **6 → 4** (the most common round is gone), and a **duplicate safe write inside one turn** is now suppressed rather than applied twice.
  - **Chronicler cadence** — `OpenRouterSettings.worldbuilding_interval` (1-10, default **2**, Config → Agents & Tools). It runs every N turns over the whole span since it last ran (`chronicler_span`, deterministic in the turn number, no bookkeeping). Halves a whole extra generation *and* reads better: judging "is this genuinely new?" from a single beat is exactly what made it re-propose the entry it wrote two turns ago. This is the front half of the open "make lore updates additive / only when there's something new" item.
  - `_HISTORY_WINDOW` **500 → 160** (~80 turns, comfortably past the largest prompt budget; 500 was ten times what any context can hold and every turn paid for the read).

  **The prompt is five tiers now** (`build_prompt`), oldest-and-general → newest-and-specific. Two rules: *every fact is stated once*, and *anything the history can contradict is stated AFTER the history*. The roster, the pack and the task list used to sit hundreds of tokens BEFORE the beats that contradict them, and the later text is the one the model believes — which is why companions who left kept getting voiced. New **PARTY roll call** (`server/ai/roster.py`) is re-read every turn and emitted **even when the party is empty** ("the player is ALONE"), because the empty case is where the history drifts most. Also a **performance** win: tier 1 is byte-identical between turns, so it stays a cacheable prompt prefix — the old order invalidated it every turn. One `CONTEXT_TURNS` window now feeds lore matching and the spotlight, so "mentioned" means one thing.

  **The clock is the server's** (`server/ai/clock.py`). `day` left the protocol entirely — it was a narrator-written integer nothing validated, free to freeze, jump or run backwards. The narrator emits a `duration` off a fixed ladder; an unknown label falls to the smallest step (garbage is unrepresentable, not clamped), a block with NO duration still advances time (an unparseable turn can't freeze the world), and `night` anchors to the next 07:00 so sleeping lands on a morning. Time reaches the player and the model as a **phase word** only. `ChatMessage.scene_minutes` (additive migration) makes reversal free; swipe/regenerate advance from where the *turn* started, so re-telling never ages the world twice. `simplify_location` keeps the narrowest segment of a stapled compound ("Boars Head Tavern - Damp Cellar" → "Damp Cellar") — which also stops the backdrop art changing when the model changes its mind about the prefix.

  **The dice are seeded** (`server/ai/dice.py`). `random.randint` behind a regenerate button is an open save-scum hole. Rolls are pure in `(turn, variant, actor, skill, difficulty)`: a regenerate re-*tells*, a swipe re-rolls. Both classic seeding traps are regression-tested — a raw FNV hash fed to `% sides` leaks its parity, and per-die suffixed seeds mean **2d6 can never roll 7**.

  **No-op writes.** An op that sets X to what X already is is not an op: a `grant_item` with no explicit count for something already stowed, an `equip` of what's already worn, an `unequip` of an empty slot, a `remove_item` for something not held. The last two are now no-ops rather than *failures* — reporting a failure invites the model to try again.

  **Android hardware Back** closes the open overlay instead of quitting the app (`client/src/lib/useBackHandler.ts`, wired into the mobile Inspector and the nav's More sheet).

  Tests: `server/tests/test_clock_dice_block.py` (25), `server/tests/test_turn_block_e2e.py` (3, through the real route with a stubbed provider), new tier-order cases in `test_prompt_builder.py`, `client/src/lib/clock.test.ts`. Server 170 passing, client 24.

  **Deliberately not done here** (next slice, in rough value order): collapsing the two narration protocols onto one lenient path with native tools as a read-only accelerator; a `reconcileBlock`-style fold of proposed state against current state (needs the writes to move out of the generation loop first); one narrator feature-flag map replacing the four scattered switches; splitting the 1800-line `SettingsPanel`; a first-run setup screen; Places; the append-only journal.

### Character system rebuild (TavernAI-2-inspired)
---
[x] Rebuild character storage as single-file SillyTavern-compatible cards, with a TavernAI-style toggleable/orderable block tree replacing the fixed basicInfo fields.
  Done: full audit of TavernAI 2's Prompt Manager / Card system against Wayward, then built the pieces worth taking. See CLAUDE.md's "Characters are portable files" bullet for the shipped shape. Short version: one PNG per character (`server/db/png_card.py` — raw `tEXt` chunk read/write, no Pillow; `chara`/`ccv3` standard chunks + `chara-ext-asset_:` asset chunks for crop/voice/extra images), a toggleable/orderable block tree (`server/ai/character_blocks.py`) with seeded `<Name>...</Name>` Open/Close Tag blocks and a live-rendered virtual Equipment block, `prompt_builder.py`'s PC/party sheet composition rewritten onto it, and import for both native Wayward cards and plain SillyTavern V2/V3 card PNGs (`import_png`, each standard field → its own block). Fixed several bugs found along the way: `RuntimeCharacter` had no `field_skill` attribute but `prompt_builder.py`/`spotlight.py` read `pm.field_skill` every turn (crashed any turn with an active party member); `add_member`/`update_member_identity` were missing the `field_skill` param their own API callers already passed; the PC sheet silently dropped Sex/Instinct via dead `gender`/`drive` key reads; `action_suggester.py` had the same dead `drive` read. Server test suite: 177/177 passing. Commit: 027aa32 (PR #43).

  **Deliberately not done here** (flagged, not silent — pick up next):
  - ~~**No block-tree UI.**~~ Done below.
  - **No Editor tool for block CRUD.** The Editor still writes the old flat fields (`create_member`/`update_member` etc.), which fold into blocks under the hood via `upsert_legacy_field` — full add/toggle/reorder/delete-block tool support is unbuilt.
  - **No upload route/UI for the `img/` extra-reference-image blocks.** Storage support exists (`set_image_asset`/`image_asset_bytes`/`remove_image_asset` in `characters.py`, `image_block_paths()` composition helper) but nothing calls it yet — and the actual vision-attachment wiring (sending enabled image blocks to a vision-capable narrator model, relying on Tier-1 prompt caching to keep it cheap) is separately unbuilt.
  - **Export-to-plain-SillyTavern** (stripping the `wayward` extension chunk, keeping only `chara`/`ccv3`) — explicitly deferred by request, not attempted.
  - **`character_book` import** (a V2/V3 card's embedded per-character lorebook) is not mapped to real Lorebook entries — silently skipped on import.
  - TTS's materialized voice-sample cache file (`voice_path_for_tts`) always names it `.wav` regardless of the real embedded format.

---
[x] Build the block-tree UI for the PC/party member sheets (the main follow-up flagged above) — a real toggleable/orderable editor instead of the old flat form.
  Done: `client/src/components/CharacterSheet/BlockTreeEditor.tsx` — a shared `BlockTreeEditor` (edit) and `BlockTreeView` (read-only Play-mode rendering), used by both `CharacterSheetEditor.tsx` and `PartyMemberEditor.tsx`. Per block: enable/disable checkbox, ▲▼ reorder, inline rename, an `ExpandableTextarea` for `text` content, a live read-only note for the virtual `equipment` block (points at the real Equipment section below it), delete-with-inline-confirm (no modal — matches the row-scale of the action), and "+ ADD BLOCK"/"+ ADD INSIDE" to create `text`/`folder`/`equipment` blocks. Folders nest **one level deep only** (no roles/depth/merge-groups, per the design note in the item above) — a new root block inserts before a trailing Close Tag if one exists, so it stays wrapped by the delimiter tags instead of landing outside them.

  **New backend routes** (`server/api/characters.py`): `PUT /player-character/blocks` and `PUT /party-members/{id}/blocks` (`{blocks, name?}`, new `CharacterBlocksUpdate` schema) persist the block tree directly via `char_files.update_blocks`; `PUT /player-character/equipment` / `PUT /party-members/{id}/equipment` save equipment alone. `PlayerCharacterResponse`/`PartyMemberResponse` now actually return `blocks` (and a pre-existing `hasVoice` field that was passed to the constructor but never declared on the schema, so it silently never reached the client — fixed as part of this same change).

  **Real bug this fixes, not just a UI gap:** `BasicInfoSchema` (the wire shape) still had the *pre-block-tree* field names (`gender`/`age`/`heightCm`/`weightKg`/`likes`/`dislikes`/`drive`) while the actual schema underneath had already moved to `sex`/`apparentAge`/`instinct`/`strengths`/`other`. The old combined `PUT /player-character` / `PUT /party-members/{id}` routes round-tripped identity through that stale schema, so **every single identity save — including a bare equipment change, since it went through the same combined payload — silently reset the Strengths/Instinct/Other blocks to empty** (pydantic drops unrecognized fields; the surviving old-shape fields then overwrote the matching blocks via `upsert_legacy_field`). Editing the block tree directly, through the new dedicated routes, sidesteps this entirely. `BasicInfoSchema` itself was also corrected to the real field names (`sex`/`apparentAge`/`instinct`/`strengths`/`other`), which incidentally fixes the old combined routes too, though the client no longer calls them. Also fixed: `HomeView.tsx`'s PC subtitle read `basicInfo.gender` (always blank, same stale-name issue) — now reads `.sex`.

  **Client:** `shared/types/models.ts` gained `CharacterBlock`/`CharacterBlockType` and a `blocks` field on `PlayerCharacter`/`PartyMember`; `BasicInfo` was corrected to the real field names. `partyStore.ts`'s `savePlayerCharacter`/`savePartyMember` (one combined save) were replaced with `savePlayerCharacterBlocks`/`savePlayerCharacterEquipment`/`savePartyMemberBlocks`/`savePartyMemberEquipment` (two independent debounced-save channels per sheet, matching the two new route pairs). The PartyMember sheet's separate "Field Skill" section (Skill Name + Skill Description, backed by the derived `fieldSkill` name/description split) is gone — Strengths is just an ordinary freeform text block in the tree now, matching the unified schema's actual shape (`fieldSkill` is kept on the wire as a read-only convenience field for now, unused by the editor).

  Manually verified end-to-end in a real browser session (Playwright against a live server + built client, not just unit tests): add/rename/toggle/reorder/delete at root and one-level-nested-in-a-folder, all round-tripping through the real API and surviving a full page reload; the read-only `BlockTreeView` renders correctly in Play mode; the Editor's own turn loop is untouched (still writes flat fields, which still fold into blocks — see the still-open item above). Server: 185/185 passing (6 new tests in `server/tests/test_character_blocks_api.py`). Client: `tsc --noEmit` clean, 24/24 vitest passing.

  **Still open, unaffected by this task:** Editor tool for direct block CRUD, image-block upload/vision wiring, export-to-plain-SillyTavern, `character_book` import, the TTS `.wav`-naming bug — all per the item above. Commit: 11c42bd.

---

[x] On PC, the left and right hand windows should be resizable horizontally, respecting a min-width and max-width to ensure Chat will still be legible. This functionality is not needed on the mobile view.
  Done: `client/src/components/Layout/AppShell.tsx` — replaced the fixed `66px_288px_minmax(0,1fr)_344px` grid with a dynamic `gridTemplateColumns` driven by `leftWidth`/`rightWidth` state, plus two `role="separator"` drag handles (thin hairline, gold on hover/active) sitting in their own 5px grid tracks between rail/left/chat/right. Pointer-drag handlers clamp each panel to its own min/max (left 220–480px, right 260–520px, both defaulting to the old 288/344) and additionally cap against `window.innerWidth` so the chat pane never drops below a 360px floor (`minmax(360px,1fr)` on the middle track) even with both panels dragged wide on a narrower window. Widths persist per-device to `localStorage` (`wayward.leftPanelWidth`/`wayward.rightPanelWidth`, same pattern as `appearanceStore`), written once on pointerup rather than every move. `MobileShell` is untouched — mobile never renders `AppShell`, so nothing there needed a min/max at all.

  Manually verified with a real dev server + Playwright: both handles drag their panel smoothly, dragging the left handle far past its max clamps at 480px without the console erroring, and a page reload after a drag restores the last-dragged width from localStorage. `tsc --noEmit` and `npm run build` both clean.

---

[x] Character Sheet UX work, part 1:

- As discussed in earlier designs, the Prompt Manager should support Nesting. When dragging an item, you can drag it between items to reorder at that layer, or drag it on top of an item to nest it under that item. Only one sub-layer allowed, so no nesting within nesting. This is mainly an UI level organisational tool, but when compiled, nested items should be marked as a smaller header, e.g. ### if the top-layer prompts are using ##.
- Let's differentiate optional Prompts (can be deleted and created at will), and Mandatory Prompts (e.g. Open Tag, Close Tag, and Equipment) which can be edited, but not moved, deleted, or disabled. We could show a lock icon on these, and perhaps use a different color for them also.
- Equipment should move inside the Equipment Prompt, making it a special type of prompt.
- The Prompt items should share a visual design with Lore Items, with the Optional Prompts showing the checkbox, and Mandatory Prompts showing the lock icon.
- Also lay the foundation work for a new Prompt type: Folder, which can hold images inside, similar to TavernAI2.

  Done: Clarified two design points with the user before building — (1) nesting-by-drop only triggers when dropping onto an existing **Folder** block, folders stay the one explicit container type (no schema change); (2) `image` already exists as its own block type (confirmed in `character_blocks.py`), so bullet 5 needed no new type — just left as-is.

  **Locked/mandatory blocks** (`server/ai/character_blocks.py`): added a `locked` bool to the block shape, defaulted `False`; `default_blocks()`/`blocks_from_legacy_basic_info()`/`blocks_from_sillytavern()` now seed Open Tag, Close Tag, and Equipment as `locked=True` (via a new `_mandatory_locked()` helper). Wire schemas (`server/api/schemas.py`) already typed `blocks` as `list[dict]`, so `locked` round-trips with zero schema changes. Client: `locked?: boolean` added to `CharacterBlock` (`shared/types/models.ts`).

  **Header-depth composition**: `compose_blocks()` now prefixes a labeled text block's content with a markdown heading — `##` at root, `###` one level into a folder — instead of the old flat `"Label: content"` line; Description/Open Tag/Close Tag stay bare (structural, not "prompts"). Updated `test_party_roster_includes_personality_and_other` in `test_prompt_builder.py` for the new format; added `server/tests/test_character_blocks.py` (4 new tests: locked-seeding, root `##`, nested `###`, bare tags/Description).

  **Lock enforcement + Lore-style restyle** (`client/src/components/CharacterSheet/BlockTreeEditor.tsx`): `BlockRow` now matches `LoreCard`'s visual language — `rounded-md`, `bg-bg2`/`bg-bg3` (open) + `SelectionBar` (lifted `LockGlyph` out of `LorePanel.tsx` into the shared `SelectionBar.tsx` so both reuse it). A locked block shows the lock glyph in place of the enable checkbox, gets a `border-gold/30 bg-gold/5` tint, can't be dragged (`draggable={!locked}`, grip dimmed with a "Locked — cannot be moved" tooltip), and has no delete control. `BlockContentInspector` (`PartyInspector.tsx`) mirrors this for the full-screen edit view — a locked block's enabled toggle is replaced with a static "Mandatory — always enabled, can't be removed or moved" note.

  **Drag-to-nest**: extended the existing native-HTML5-DnD reorder machinery (no library added) with a Y-position band on root Folder rows — the header strip's middle ~50% is a "nest into this folder" target, the edge bands stay plain reorder. New `removeBlockAt`/`insertBlockAt`/`appendToFolder` tree helpers support the resulting cross-scope moves (root↔folder, folder↔folder). A folder can never be dropped into another folder (one sub-layer only — enforced in both the dragover band-detection and the move handlers), and a locked block can't be dragged at all. **Real bug caught during manual verification and fixed before landing**: the nest-band Y-fraction was first computed against the whole row's (card's) bounding rect, which for an *expanded* folder includes its children + "+ ADD INSIDE" button — making the nest band tiny and mispositioned relative to what the user sees as the folder's header. Fixed by moving the `onDragOver`/`onDrop` listeners (+ `stopPropagation`, so a nested child row's drag events don't bubble into its parent Folder's handlers) onto the header strip specifically, not the whole card.

  **Equipment moved into the Equipment prompt**: extracted the ~150-line equipment slot grid (`EQUIP_SLOTS`, `EquipSlotField`, rarity maps) — previously byte-identical duplicated code in `CharacterSheetEditor.tsx` and `PartyMemberEditor.tsx`, each rendering it in **two** separate standalone "Equipment" sections (view mode + edit mode) outside the block tree — into a new shared `client/src/components/CharacterSheet/EquipmentGrid.tsx`. Both standalone sections are gone; the Equipment block/row is now the only place equipment is edited: `BlockContentInspector`'s equipment branch renders the real `<EquipmentGrid>` (was a static "see the Equipment section below" note) for the full-screen Edit-mode view, and `BlockTreeView` (the read-only Play-mode renderer) renders it inline in the flattened list — equipment stays interactive there since managing gear is a play action, not world-editing.

  **Discovered while verifying, out of scope, flagged not silently fixed**: `PartyInspector.tsx`'s `mode` is currently `const mode: 'view' | 'edit' = 'edit'` — hardcoded, per its own comment, since a prior refactor ("Full CRUD is always available now — no separate Edit Mode gates it."). This means `BlockTreeView`/the Inspector's `'view'` branches are presently unreachable through the live app's own navigation. Verified `BlockTreeView`'s new equipment-inline rendering correctly by temporarily forcing `mode` to `'view'` for a screenshot, then reverting — did not change this architectural decision, as it's outside this task's scope.

  Manually verified end-to-end against a real dev server + Playwright (native HTML5 DnD, so `locator.dragTo()`, not raw mouse events, which don't reliably trigger real browser drag sessions): dragging a text block onto a Folder nests it (children render indented with their own lock/style treatment); dragging a locked block's grip is a no-op; dragging one Folder onto another reorders them at root without nesting (folders never accept a folder child); opening the Equipment block full-screen shows the real 12-slot grid with live equipped items, unequipping there persists through a page reload; the read-only Play-mode view renders the same grid inline. Server: 189/189 passing (185 existing + 4 new). Client: `tsc --noEmit` clean, 24/24 vitest passing.

  **Deliberately not done** (flagged, not silent): server-side enforcement of `locked` on block-tree writes (diffing old vs new tree) — this is UI-level organization per the task's own framing, and the only mutation path is "save the whole tree," so enforcing would need a real diff for little current benefit; revisit if the Editor ever gets direct block-CRUD tools (still on the earlier character-system rebuild's open list). No new block type and no image-upload UI — confirmed unnecessary/out of scope with the user. Root/folder "+ ADD" menus unchanged (no new type this round).

  **Follow-up, same task**: the `locked` field only applied to *newly-seeded* blocks — an existing character's blocks (Varena, the fantasy template's bundled party member, included) predate it and were unaffected. Turned this into a proper one-time migration rather than just hand-patching Varena: `server/ai/character_blocks.py` gained `backfill_locked(blocks)` (mutates in place, root + one level into folders, only fills in a *missing* `locked` key so it never fights a real edit); `server/db/characters.py` bumped `SCHEMA_VERSION` 3→4 and `read_character()` now runs the backfill + rewrites the card whenever a stored `wayward.schemaVersion` is behind current, mirroring the existing `_migrate_legacy_folder` on-first-read pattern. This fixes every pre-existing character on any install (not just Varena) the first time it's read after upgrading — no separate fix to `install_bundled_cards()` needed, since that function's `exists(cid): continue` guard was the actual reason Varena's bundled card never got regenerated. New test `test_pre_lock_field_cards_get_locked_backfilled_on_read` (`server/tests/test_character_identity.py`) hand-writes a pre-v4-shaped card and asserts the backfill + version bump + idempotency; bumped the existing `test_create_and_read_uses_new_schema` assertion from `schemaVersion == 3` to `4`. Verified against this dev machine's real (quite old — pre-dates several other block-tree changes) local Varena card via a live server: `locked` now shows `true` on her Open Tag/Equipment blocks. Server: 190/190 passing. Commit: a6eb3e8 (+ follow-up, see below).

  ---
  [ ] Equipment UX improvements & item generation to work in both directions:

  - 