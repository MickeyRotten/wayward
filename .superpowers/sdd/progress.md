# SDD Progress Ledger
Task 1.1: complete (commits d7ee39d..4dd95d0, review clean)
Task 1.2: complete (commits 4dd95d0..dd8bfa8, review clean — minor: dead prevTabRef, type assertion in PartyInspector)
Task 1.3: complete (commits dd8bfa8..869b2cd, review clean)
Task 1.4: complete (commits 869b2cd..d658f96, review approved — implementer also restored adventure endpoints and sampling params from uncommitted working copy)
Task 2.1: complete (commits d658f96..c53eb64, review approved — slot vocab is by-design per spec)
Task 2.2: complete (commits c53eb64..f35dab9, review approved — minor: no count>0 validation on inventory add/remove)
Task 2.3: complete (commits f35dab9..34320c4, review approved)
Task 2.4: complete (commits 34320c4..77cd4fc, review approved — slot compat filtering is intentionally loose per spec)
Task 3.1: complete (commits 77cd4fc..a340eb1, review approved — minor: prompt assembly comment numbering)
Task 3.2: complete (commits a340eb1..ce0c3e9, review approved — minor: defaultValue vs controlled on status dropdown, same pattern as ItemInspector)
Task 4.1: complete (commits ce0c3e9..7c2bf8c, review approved)
Task 4.2: complete (commits 7c2bf8c..56a594e, review approved)
Task 5.1: complete (commits 56a594e..cc6b023, review approved — reviewer's Critical findings verified as non-issues: portrait field exists, rendering uses role not speaker)
Task 5.2: complete (commits cc6b023..b273de1, review approved)
Task 5.3: complete (commits b273de1..190f064, review approved)
Task 6.1: complete (commits 190f064..44e8145, review approved)
Task 6.2: complete (commits 44e8145..0614f99, review found 1 Critical [C1] fixed — player deltas now deferred to save transaction so failed/cancelled generation can't orphan/double-apply. Minor ledger notes: M1 non-active variant notice can show non-live deltas; M2 delete-truncate reverses only most-recent msg [spec-sanctioned]; M3 substring/global-verb matching in detect_item_use; M4 done-event delta payload unused client-side; M5 items panel not refreshed after turn. Client TS build unverified — node not installed in env; types/imports manually confirmed consistent.)
Task 7.1: complete (commit 9dc0bec, review APPROVED — also fixed latent bug: old saveSettings PUT omitted sampling params, silently resetting them server-side each save; now round-trips. Settings modal migrated to left-panel Config tab. Minor ledger notes: M1 Lorebook injection Order number inputs PUT per-keystroke [debounce/save-on-blur later]; M2 React.ReactNode used without import [matches existing codebase pattern]. Client TS build unverified — node not installed; types/round-trips manually confirmed.)
Task 7.2: complete (commit 9830c33, self-reviewed — trivial static stub. ScenePOIList reads scenario from narratorStore, 3 hand-authored POIs with local-state inline detail, no global inspector wiring/no new SelectionKind. Orphaned TabPlaceholder removed, no dangling refs. Client TS build unverified — node not installed; types/imports manually confirmed.)
TOOLCHAIN: Node 24.17.0 + npm 11.13.0 installed portable to %LOCALAPPDATA%\nodejs (persistent user PATH). Server venv at server/.venv (Python 3.14.4). FIRST client build (commit b9fe62d) caught 3 real TS errors static review missed: IconRail JSX namespace (->ReactNode), dead RARITY_COLORS in PartyInspector, vestigial onRegenerate chain in ChatScene. Also added missing python-multipart to requirements.txt (commit 1d7dcab). Server boots+seeds clean; routes under /api. Client builds clean from here on — build verification now IN the loop.
Task 8.1: complete (commit b2534c9, self-reviewed + build-verified. Chat header in ChatScene.tsx: flex-shrink-0 region, bg-bg2 + dot-grain radial-gradient, Cinzel gold location name [reuses deriveLocationName from Task 7.2], Scenario/History pill toggle. Toggle has minimal real behavior: Scenario tab shows scenario blurb, History shows messages [list kept mounted via 'hidden' to preserve scroll]. Minor: input area stays active on Scenario tab [acceptable]. npm run build passes.)
Task 8.2: complete (commit 20ede1f, self-reviewed + build-verified. CSS-only: added .font-disp-corrected utility to index.css. Audit found codebase already consistent — every font-disp element already carries an appropriate inline pt- correction from prior tasks; no old tokens (bg-off/text-text-sec/border-border/font-h etc.) anywhere [grep-verified clean]; gold/border/text-hierarchy tokens consistent. Drop-cap's mt-[0.15rem] left as deliberate float offset. npm run build passes.)
Task 9.1: complete (fix commit 1c635a9 + this ledger. Full end-to-end verification with live OpenRouter key against fresh-seeded DB. FOUND+FIXED real integration bug: chat_completion_stream() rejected top_p/min_p/top_k/freq/pres/rep penalties that the route passes from settings — every live chat turn failed. Now forwards non-no-op values to OpenRouter. ALSO: requirements.txt python-multipart fix from toolchain setup (commit 1d7dcab) was part of making server runnable. Verified PASS: seed (Seraphine+Tifa+Rosalina, 20 catalog items across all types, 3 quests w/ objectives, 10 lore across 5 cats, narrator+scenario, starter inv); model list loads (340); settings persist+respected; chat end-to-end; spotlight direct-address forces response (spot=Directly addressed) AND silence-default (neutral->narrator only); prompt assembly injects SPOTLIGHT+QUESTS+LOREBOOK(keyword+permanent match)+ACTION instr; item-use detect decrements + records delta + idempotent reversal on regenerate (x2->x1, regen stays x1); narrator action exec path proven (parse_action_block strips block, execute_actions performs equip swap+records change; addItems graceful when full); CRUD persist (scenario/narrator/PC/party add+remove); inventory stacking, max-stack(400) + carry-capacity(stacks-not-units, 400) enforced; message edit/swipe/delete-and-after all work. KNOWN MINORS (non-blocking): failed/aborted stream leaves orphaned user message (pre-existing); narrator live item-grant is model-dependent (weak claude-3-haiku didn't always emit block — mechanism verified directly). DB reset to clean seed + API key scrubbed after testing. Client build green. ALL 'Definition of Alpha Done' items verified.)
